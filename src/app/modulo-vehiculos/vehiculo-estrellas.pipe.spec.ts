import { VehiculoEstrellasPipe } from './vehiculo-estrellas.pipe';

describe('VehiculoEstrellasPipe', () => {
  it('create an instance', () => {
    const pipe = new VehiculoEstrellasPipe();
    expect(pipe).toBeTruthy();
  });
});
